# Research Coverage Category

Future child layers are planned as:

- Matching research by administrative area;
- Explicit research footprints;
- Selected research-object coverage.

Each child remains independently toggleable and uses the same disclosure/visible-count pattern as the existing map categories.
