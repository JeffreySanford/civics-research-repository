# Ready Definition

The draft can become ready only after the spatial foundation is stable and the bounded API, Research Coverage layers/category, shared map/list state, cursor-backed result lists and browser/manual accessibility evidence exist.
