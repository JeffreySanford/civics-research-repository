# C2 Map Boundary

The million-record corpus is a search/evidence corpus, not a million-point map layer. Map integration must aggregate administrative coverage and bound explicit features by viewport/detail caps. Associated research lists may traverse deeply through cursor semantics, but the canvas never receives the full result set.
