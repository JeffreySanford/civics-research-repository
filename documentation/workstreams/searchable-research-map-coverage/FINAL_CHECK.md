# Final Check Before Merge

Do not mark this draft ready until the spatial foundation is stable and the bounded APIs, Research Coverage category/layers, shared selection state, cursor-backed lists and browser/manual accessibility evidence are implemented.
