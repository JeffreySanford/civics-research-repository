# Accessibility Contract

Every research-coverage map value has an equivalent semantic representation. Map/list selection shares state; map-originated selection has an announcement/semantic target; no task exists only through canvas pointer/drag interaction; disclosure/layer controls remain keyboard operable; and reflow/zoom/forced-colors behavior stays within the existing evidence model.
