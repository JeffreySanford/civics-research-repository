# Future Extension

When research spatial coverage is implemented, add a peer `Research Coverage` disclosure rather than appending those controls to either existing category. This keeps research-object provenance visually and semantically distinct from Census/community context and USGS/environment/hazard context.
