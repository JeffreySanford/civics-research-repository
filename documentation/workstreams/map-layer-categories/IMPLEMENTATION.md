# Implementation Sequence

1. Group existing controls under native disclosure categories.
2. Preserve every existing checkbox, URL parameter and NgRx action.
3. Add textual active-child counts to category summaries.
4. Style disclosures with existing design tokens and forced-colors behavior.
5. Add browser evidence proving collapse does not change MapLibre visibility.
6. Retain existing semantic layer lists/tables and tooltip accessible descriptions.
7. Reserve a separate future Research Coverage category rather than mixing federated research with reference layers.
