# Ready Definition

This implementation PR is ready to merge only after standard CI/browser checks pass. The branch already contains the intended code/test/docs scope; CI is the remaining merge gate.
