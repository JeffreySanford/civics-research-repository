# Accessibility Contract

Native disclosure semantics organize controls without replacing them. Existing labelled checkboxes, tooltip accessible descriptions, semantic layer lists/tables, live announcements and forced-colors meaning remain intact. Category summaries add text-only active counts and must retain visible focus at reflow/zoom sizes.
