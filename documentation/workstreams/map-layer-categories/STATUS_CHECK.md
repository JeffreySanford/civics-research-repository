# Expected CI Gates

The PR should run the normal Workspace/Repository API/Browser Evidence/GitGuardian checks. The new browser assertion is tagged `@maps @wcag @section508` so it participates in the existing evidence routing rather than creating a parallel accessibility suite.
