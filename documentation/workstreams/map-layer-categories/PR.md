# Pull Request Summary

Group the six existing map-layer controls into two expandable categories while preserving every child checkbox, URL/NgRx state, MapLibre rendering contract and accessible equivalent. Add active-child counts, tokenized disclosure styling and browser evidence that collapse does not mutate render state.
