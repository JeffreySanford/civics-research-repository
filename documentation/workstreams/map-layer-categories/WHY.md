# Why Now

The current six-layer flat list is already hard to scan, and planned Research Coverage layers would make it worse. Introducing category semantics before new spatial research layers arrive prevents a later UI rewrite and gives future layers an accessibility-tested home.
