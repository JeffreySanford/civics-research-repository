# Merge Order

The spatial foundation should merge before searchable Research Coverage implementation. It is independent of the map-category UI organization but should reuse that category pattern once both are on main.
