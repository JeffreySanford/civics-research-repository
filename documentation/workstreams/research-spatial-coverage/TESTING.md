# Testing

Evidence should include domain validation, persistence round-trips, source-specific fixture extraction, invalid geometry rejection, deterministic gazetteer mapping where used, proof that publisher location is not treated as research coverage, OpenAPI/generated-client drift checks and semantic presentation tests for exposed coverage/provenance.
