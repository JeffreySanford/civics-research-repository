# Identity Boundary

Spatial enrichment is treated as application-owned sidecar evidence for the current C2 corpus. That lets research geography improve without silently changing the already-certified retained composition and projection identity. A future corpus version may deliberately include spatial semantics in its identity, but that requires an explicit versioned decision.
