# Non-goals

This work does not infer research geography from publisher/institution location, render spatial data before provenance is modeled, replace application-owned evidence with search-engine state, or silently alter the certified C2 composition.
