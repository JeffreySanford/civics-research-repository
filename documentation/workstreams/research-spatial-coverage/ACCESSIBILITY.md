# Accessibility Contract

Spatial coverage is incomplete unless meaningful area/geometry/provenance information can be presented semantically without the canvas. The domain/API model must carry enough label, value and provenance data for later accessible lists/tables and keyboard selection paths.
