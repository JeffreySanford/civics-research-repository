# Future Consumers

The next searchable-map workstream should consume these coverage records through bounded APIs. Later PI-6 geo search may project the same authoritative model into Solr/OpenSearch without making either engine the source of spatial truth.
