# C2 Evidence

The live proof should traverse bounded windows of the exact active `FEDERATED_1M` projection in both engine modes, record projection identity and ordering criteria, and fail evidence capture if duplicate/skip defects or cursor/projection mismatches occur.
