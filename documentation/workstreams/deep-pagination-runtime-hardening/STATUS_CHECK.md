# Expected CI Gates

Once implementation lands, the PR should remain under the standard workspace/API/browser evidence checks plus explicit live C2 cursor evidence. Million-record traversal remains explicit research evidence rather than ordinary PR workload where runtime cost is inappropriate.
