# Merge Order

This is the next PI-1 runtime-hardening branch. It can proceed independently of map category organization. Research-map result lists should consume its cursor contract after it stabilizes.
