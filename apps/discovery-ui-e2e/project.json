{
  "name": "discovery-ui-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/discovery-ui-e2e/src",
  "implicitDependencies": ["discovery-ui"],
  "// targets": "to see all targets run: nx show project discovery-ui-e2e --web",
  "targets": {
    "accessibility": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec playwright test --config=apps/discovery-ui-e2e/playwright.config.mts --grep @wcag"
      }
    },
    "wcag": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec playwright test --config=apps/discovery-ui-e2e/playwright.config.mts --grep @wcag"
      }
    },
    "section508": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec playwright test --config=apps/discovery-ui-e2e/playwright.config.mts --grep @section508"
      }
    }
  }
}
