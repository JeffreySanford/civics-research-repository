name: Mobile-first PR4 formatter

on:
  push:
    branches:
      - codex/mobile-first-census-pr4-pagination-ranking
    paths:
      - .github/workflows/mobile-first-pr4-format.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: codex/mobile-first-census-pr4-pagination-ranking
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Apply repository Prettier
        run: pnpm exec prettier --write apps/census-mobile-frontend/src/app/app.html apps/census-mobile-frontend/src/app/app.ts apps/census-mobile-frontend/src/app/state/search/search.effects.ts
      - name: Commit canonical formatting and remove formatter
        run: |
          rm .github/workflows/mobile-first-pr4-format.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: apply canonical PR4 mobile formatting"
          git push origin HEAD:codex/mobile-first-census-pr4-pagination-ranking
